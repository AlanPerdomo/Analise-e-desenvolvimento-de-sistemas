# 16 - Ler dois números que representam a base a altura de um triângulo, imprimir a área

altura = float(input("Digite a altura do triangulo: "))
base = float(input("Digite a base do triangulo: "))

print("A area do triangulo é "+str((altura*base)/2))